[package]
name = "portal-proto"
version.workspace = true
edition.workspace = true
description = "portal wire protocol: framed CBOR envelopes, v4-compatible with the Go implementation (pkg/protocol)"

[dependencies]
serde = { workspace = true }
serde_bytes = { workspace = true }
ciborium = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true, optional = true }

[features]
default = []
async = ["dep:tokio"]

[dev-dependencies]
hex = { workspace = true }

[package]
name = "portal-client"
version.workspace = true
edition.workspace = true
description = "Client for Portal's owner-only local control API"

[dependencies]
portal-core = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }

[dev-dependencies]
tempfile = "3"
